package org.gio.iam.manual;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean; // Import this
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.security.oauth2.jwt.JwtDecoder; // Import this

import java.util.TimeZone;
import static org.junit.jupiter.api.Assertions.assertEquals;

// 1. Add 'webEnvironment = NONE' to prevent server startup issues
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.NONE)
public class RedisConnectionTest {

    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    // 2. THIS IS THE FIX: Mock the missing JwtDecoder bean directly
    @MockBean
    private JwtDecoder jwtDecoder;

    @Test
    public void testRedisWriteAndRead() {
        TimeZone.setDefault(TimeZone.getTimeZone("UTC"));
        System.out.println("🧪 TEST: Attempting Redis connection...");

        redisTemplate.opsForValue().set("test:key", "test_value");
        Object value = redisTemplate.opsForValue().get("test:key");

        System.out.println("✅ TEST SUCCESS: Retrieved value: " + value);
        assertEquals("test_value", value);
    }
}